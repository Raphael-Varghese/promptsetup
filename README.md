# promptsetup

A custom CLI styling and prompt library.

## Installation

```bash
pip install promptsetup
```

## Usage

```python
from promptsetup import Fore, prompt
text = prompt("192.168.1.1", "user", "linux", "kali")
terminal_prompt = input(text)
```
